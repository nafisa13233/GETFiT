@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right " }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">

            <li class="nav-item dropdown">
               
                <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"> @User.Identity.Name </a>
                <div class="dropdown-menu">
                    @Html.ActionLink("View Profile", "MyProfile", "Account", new { area = "" }, new { @class = "dropdown-item nav-link" })
                    @Html.ActionLink("Appointments", "Appointments", "Account", new { area = "" }, new { @class = "dropdown-item nav-link" })
                </div>               
            </li>

            <li><a href="javascript:document.getElementById('logoutForm').submit()" class="nav-link">Sign Out</a></li>
        </ul>
    }
}
else
{
    <ul class="navbar-nav">
        <li class="nav-item">
            @Html.ActionLink("Sign In", "LoginOption", "Home", routeValues: new { area = "" }, htmlAttributes: new { id = "loginLink", @class = "nav-link" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("Sign Up", "Register", "Account", routeValues: new { area = "" }, htmlAttributes: new { id = "registerLink", @class = "nav-link" })
        </li>
    </ul>
}
